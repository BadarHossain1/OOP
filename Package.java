public class Package {
    
    //packages are just containers for classes, keeps the class name in compartments. In simple terms package is just a folder. 
    

    //               Package
    //               /     \
        // User Defined     In built


    // Java --> lang, io, util(data structures, collection frameworks), applet(For Development), awt(GUI), net(Network connection)
}
